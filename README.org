  
  Language-python
  Libraries needed- Spacy, Neuralcoref,pandas and tqdm using pip
  run - "example.py" for cloze test/ 'chain.py" has main code for system.
  "example.py" is running on "all.json" which is the prebuilt model. use "train.csv" to build training set if wanted.
  load_data has sample parameter to finetune the trainingset/testset.

  Thank you :D
